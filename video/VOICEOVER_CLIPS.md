# Solvent demo — voiceover script

Rules honored: captions are byte-for-byte identical to these lines. No em-dashes anywhere.
Show, don't sell. Lead with the strongest element (live on-chain proof). ~55s total.

## Clip 1 — hero (ring fills to SOLVENT)
This issuer just proved it can cover every customer. On chain. Without revealing a single balance.

## Clip 2 — prove (money shot: balances hide, total proven)
Type in the private customer balances. The browser builds a zero knowledge proof that they sum to a public total. The balances stay hidden. Only the total is proven.

## Clip 3 — board (on-chain verdict)
A Stellar contract verifies that proof with native cryptography. Then it confirms reserves cover the proven total. The result reads solvent, live on chain.

## Clip 4 — how (the guarantee + close)
The whole point is that you cannot lie. Change the total, and the cryptographic check fails. This is proof of reserves you can actually verify.
